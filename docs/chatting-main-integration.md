# 대화 개선과 main 통합 — 2026-09-16

통합 기준 main: `20ae128`. 대화 브랜치의 기존 HEAD: `e7eb2d2`.
사용자 승인에 따라 main의 번호를 유지하고 대화 패치 026/027/028/040을
047/048/049/050으로 정정한다. 과거 커밋·평가 원본 파일명은 재현을 위해 보존한다.

## 보존한 기능과 통합 변경

- 회원 채팅방·마이페이지·문서 저장·PDF 리포트와 최신 인용 검증을 유지한다.
- 의도·목적 판단, 사용자 진술 기억, 안내 뒤 확인 질문, 후속 질문을 통합한다.
- API 응답의 출처·용어 설명·추천 질문에 대화 상태를 함께 제공한다.
- 답변 줄바꿈을 적용한 뒤 인용 위치를 계산해 근거 표시가 어긋나지 않게 한다.
- 회원 대화 초기화와 문서 삭제에서 파생 대화 상태도 비운다.
- 첫 회원 채팅방 생성 후 작성 중인 다음 질문이 있으면 자동 새로고침하지 않아 초안을 보존한다.
- request identity와 현재 확인 질문 ID 검사를 유지한다.

## 실행

`python manage.py migrate` 후 `CHAT_CONVERSATION_ENABLED=true`로 실행한다.
공유 기본값 false는 기존 경로로 복귀할 수 있도록 유지한다. 로컬 개발 서버는 true로 실행한다.
현재 로컬 DB는 마이그레이션 전 SQLite backup API로 백업했고 기존 대화를 보존했다.
암호화 원본 저장 설정은 main의 기존 안내를 따른다. 비밀 환경값은 커밋하지 않는다.

## 검증과 한계

전체 Python 2,373건·678 subtests 통과, 선택 환경 검사 3건 생략.
회원 대화·초기화·웹·가이드 관련 98건, LLM 설정·대화 라우팅 83건(28 subtests),
JavaScript 전송·재시도·초안 유지 9건 통과. Django check 및 migration drift 검사 통과.
브라우저 새로고침 후 기존 대화와 회원 메뉴·대화 컨트롤 표시를 확인했다.
첫 실제 통합 실행은 일반 안내·출처·후속 질문까지 통과했으나 다음 턴의 planner가
35초 제한에 걸렸다. 부분 결과 `patch050-main-integration.json`을 보존한다.
이 결과는 전체 대화 성공으로 집계하지 않는다. 실제 모델의 지연·시간 초과와
다른 주제의 안내 자료 확장·독립 품질 평가는 후속 개선 범위다.

추가 진단에서 같은 날짜 입력은 준비된 모델에서 26.25초에 정상 구조를 반환했다.
생성 이후 문맥 전환 시의 반복 시간 초과를 고려해 planner 제한만 35→60초로 조정했다.
전체 요청 시간·호출 수 상한과 실패 시 상태 보존은 그대로 유지한다.

시간 제한 보완 후 관련 153건을 재검증했다. 추가 줄바꿈·인용 위치 회귀를 포함한
가이드 검사 10건도 통과했다. main에서 그대로 가져온 파일의 기존 공백 경고 1건은
범위 밖으로 보존했으며 main 대비 통합 변경의 diff 검사에는 오류가 없다.

최종 실제 3턴(`patch050-main-integration-03.json`)은 모든 평가 조건 및 소스 일치
검사를 통과했다. 안내+종료일 질문 158.09초, 날짜 저장+다음 질문 51.99초,
문자 요청 방법 답변 121.60초였다. 이 결과는 기능 통과이며 성능 개선을 의미하지 않는다.

통합 구현 커밋: `1d8238f`. 대화 개선 구현 범위를 완료 처리하고 성능·확장 평가는 후속으로 분리한다.
