# PR 제목

fix(generation): 검색 조문 근거와 마크다운 인용 검증 보완 (PATCH-039)

# PR 본문

검색 청크 본문의 교차참조만으로 미검색 조문 인용이 통과하던 문제와, 법령명·조 번호 사이의 강조 표시 때문에 정상 인용이 누락되던 문제를 수정합니다.

- 법령명 해석용 문맥과 실제 검색 조문 허용 목록을 분리합니다. 별도 검색된 참조 조문은 해당 청크로 정상 인정합니다.
- 강조 구분자를 같은 길이의 검사용 공백으로 처리해 원문·오류 위치를 보존합니다. 항 목록·범위에도 적용해 기존 F-06 검사를 유지합니다.
- PR 리뷰의 연속 인용 누락을 정정했습니다. 조문별로 추출 범위를 제한해 `**민법 제3조**과 **민법 제4조**` 및 일반 문장에서 각 조문을 검사합니다. 9개 표기·5개 연결 표현·3개 보유 조건을 검증했습니다.
- chain/graph의 잘못된 인용 보류·semantic judge 미호출과 정상 강조 인용 통과를 확인했습니다. 관련 **366 passed, 6 subtests passed**입니다.

전체 테스트는 `.venv/Scripts/python -X utf8 -m pytest -q`로 **1,413 passed, 3 skipped, 172 subtests passed**입니다. 기본 Windows cp949 모드에서는 기존 `test_progress_js.py`의 한글 파일 읽기가 실패해 UTF-8 모드로 전수 재검증했습니다. 실제 LLM 27문항 및 답변율·지연·비용 재측정은 미실시입니다. 교차참조 대상 본문이 없는 정상적인 관계 설명도 보류될 수 있어 실제 답변 회귀 확인이 필요합니다. 검색·DB·모델·프롬프트 변경은 없습니다.

의존: PATCH-022. 상세 범위와 한계는 `docs/patch039-citation-grounding.md`에 기록했습니다.
