# PATCH-054 인사 이후 상담과 종료일 응답 연결

2026-09-16 사용자가 제시한 실패는 PATCH-050 누락이 아니라 분기 조건 문제였다.
저장 상태에서 연장 요청이 greeting/social로 분류됐고, `2달 남았어`는 end_date에
정상 저장됐으나 다음 질문이 없는 rag로 실행되어 법률 검증이 보류됐다.

- 현재 발화의 상담 목적을 이전 인사보다 우선하며 물음표 없는 요구도 상담으로 판단하도록 보완했다.
- 안내 뒤 대기 질문에 대한 정보 답변이면 모델이 다음 필드를 생략해도 미확인 항목 하나를 선택한다.
- 검증된 선행 답변이 있을 때만 정보 저장 확인으로 이어가며, 새로운 실질 질문과 검증 보류 응답에는 기존 RAG·검증 규칙을 유지한다.
- 사용자 진술을 실제 날짜로 계산하거나 법률 적용 조건 충족으로 단정하지 않는다.

실제 모델의 `안녕 → 임대차 계약이 곧 만료될거라 연장을 하고 싶어 → 2달 남았어`
흐름에서 social → rag/procedure+end_date 질문 → clarify/landlord_notified 질문을 확인했다.
자료: `data/eval/chatting/results/patch054-planner-replay.json`.
법률 응답은 고정 대역이며 이 실행은 실제 법률 답변 품질·검색 성능 평가가 아니다.
상대적 기간 두 표현과 HTTP 상태 저장·다음 질문 연결 회귀를 추가했다.

전체 Python 검사: 2,376 passed, 3 skipped, 678 subtests passed. Django check 통과.
9월 16일 검증 당시에는 로컬 서버에 적용했고 커밋·푸시·main 병합 전이었다.

## 통지 여부의 후술 답변 보완

사용자 재현의 `아니 아직 알려주지 않았어`는 `invalid_decision:unstated_notification`으로
거절됐다. 통지 동사의 활용형 `알려`가 검증 패턴에서 누락된 것이 원인이었다.
보고절·관련 절 선택·통지 진술 검사에 같은 활용형을 반영하고, `안 알려…` 부정도 검사한다.
통지하지 않았다는 진술 4개와 부정 의사를 통지했다는 진술 2개를 추가 검증했다.
관련 계약·정규화·대화·API 회귀 239건 통과. 법률 검증 기준은 변경하지 않았다.

실제 의도 판단 모델에서도 같은 답변을 landlord_notified=아니요로 저장하고,
맥락 확인 복구 질문 없이 기존 계약 갱신 상담의 RAG 경로로 이어짐을 확인했다.
자료: `data/eval/chatting/results/patch054-notification-replay.json`.
이 재현은 법률 응답을 고정 대역으로 사용했으며 최종 법률 답변의 품질 평가는 아니다.

2026-09-17: 원격 번호 051~053 보존을 위해 이 미커밋 대화 패치를 PATCH-054로 정정했다. 이전 실행 결과의 내용은 보존하고 파일명·문서 참조만 맞췄다.

최신 origin/main 4491b76 통합 후 전체 Python 2,680 passed, 3 skipped, 678 subtests passed (148.60초). 생략: 선택 LangSmith 2건, 실물 등기 PDF 1건. JavaScript 입력창 4건·Django check·diff check 통과. 이번 통합에서는 실제 외부 모델·실물 OCR 평가를 재실행하지 않았다.

구현 커밋: 8ed7546. PATCH-054 완료 기록을 동기화했다.
