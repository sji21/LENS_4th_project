# PATCH-028 로컬 DB 적용 결과

현재 기본 설치는 [승인 원천 기반 서버 구축](server-data-setup.md)입니다. 아래 초기 DB 묶음 적용·235입력 기록은 평가 재현용 이력으로 보존하며, 일반 서버 구축 결과는 마지막 절에서 구분합니다.

## 완료 상태

`C:/team_project/4th_project/data/`에 일반 법령178·민법26, 총204조문을 실제 적용했습니다. 판례26·안내6은 유지합니다. 사용 코드는 최신 main 기반 `feat/patch-028-local-data-setup`입니다. 간편 명령 사용법은 [로컬 데이터 적용](local-retrieval-data.md)을 참고하세요.

## 실행·검증

- 구현: `b3669e1`. 실제 적재·검색 실행: `50c4ed8` clean. 상태 검사의 Chroma 사본 사용 보완: `e8e3a61`.
- 명령: `.\manage-data.ps1 apply -Source C:/team_project/patch026-worktree/data`.
- 사전235입력·기본 경로 사후235입력 모두 PATCH-027 최종 결과와 네 채널의 순위·본문·출처 일치. 각각 KURE375호출, 평균 약0.396초. 새 독립 평가나 LLM 답변 평가는 아닙니다.
- 기존 데이터 백업: `tmp/retrieval-data/20260913T065737Z-21385343/backup`. 사전/사후 결과와 백업 영수증은 [공유 검증 자료](../data/eval/patch028-rollout/summary.json)에 연결했습니다.
- 사본 검사 보완 후 같은 `apply`를 원본 인자 없이 재실행했습니다. 전체 DB·청크·Chroma·프로필 바이트가 동일하고 새 실행 폴더·백업 생성이 없었습니다.
- 최초 재실행 검사에서는 조문 수·논리 인덱스가 유지됐지만 Chroma 조회가 내부 파일을 다시 저장했습니다. 해당 방식의 바이트 무변경 검증은 실패했으며, 사본 검사로 수정한 후 재검증 통과했습니다. 사전/사후235입력 실측은 이 보완 전 커밋에서 실행된 기록이며 덮어쓰지 않았습니다.
- 메인 적용 후 전체 **1,135 passed, 3 skipped, 172 subtests passed**. 이후 사본 검사 보완을 포함한 관련 **20 passed**. 최종 전체 검사에 새 테스트1개가 포함됐다고 합산하지 않습니다. 건너뜀은 LangSmith미설정2·로컬PDF미제공1입니다.
- 최초 구현은 검증된143→204 묶음 적용용이었습니다. 이후 아래 보완에서 빈 환경 최초 설치를 추가했습니다. 다른 데이터/판본은 명시적인 검토 없이 교체하지 않습니다.

## 기존 로컬 변경 보존

원래 기본 작업 폴더는 PATCH-024 브랜치에 미커밋 변경5개가 있었습니다. 해당 파일 원본·diff·브랜치정보는 `tmp/patch028-preserved-patch024/`에 백업했고 `preserve PATCH-024 local review before PATCH-028 rollout` Git stash에도 보관했습니다. 이번 패치에 섞거나 삭제하지 않았습니다.

## 기본 실행 안내 보완

인자 없는 `manage-data.ps1` 실행을 DB 체크→필요 데이터 적용→확인으로 변경했습니다. 현재 적용 상태에서 세 단계와 추가 불필요·변경 없음 표시를 실제 확인했습니다. 원본 폴더는 필요할 때만 입력받으며 상세 실행 로그는 파일로 보존합니다. 관련24개 테스트 통과, 제품 검색·적재 데이터 변경 없음.

## 신규 팀원 준비·최초 설치 보완

- README6절에서 `setup_data.py` 간편 실행을 먼저 안내합니다. Python3.11 가상환경 생성·필요 모듈 설치·의존성/모듈 로딩 검사·검증 버전 KURE 다운로드 후 DB 작업을 실행합니다. 이미 있는 모델은 네트워크 다운로드 없이 파일을 검사합니다.
- ‘검증된 확대 데이터 폴더’가 Git 외부로 전달하는 SQLite·청크·두 Chroma 인덱스·프로필 묶음임을 설명했습니다. 담당자가 전달할 ZIP은 `lens-data-204.zip`(2,335,239바이트)이며 [최초 설치 요약](../data/eval/patch028-rollout/fresh-install-summary.json)에 SHA-256을 기록했습니다. ZIP 자체는 로컬 `tmp/team-data-handoff/`에 준비했으며 원격 업로드하지 않았습니다.
- 빈 DB 별도 작업 폴더에서 `9aadb63` 코드로 최초 설치를 실행했습니다. 사전235/235·사후235/235입력 일치, 각각 KURE375호출. 기존 메인 DB는 재적재하지 않았습니다.
- 전체1,153통과·3건너뜀·172subtests. 이후 `e59f53f`의 모델 다운로드/캐시 경계 보완을 포함한 관련41통과. 현재 Windows 환경의 `setup_data.py --check`도 성공했습니다.
- 실제 최초 설치는 기존 Windows 가상환경·모델 캐시를 사용했습니다. 새 가상환경의 패키지 설치와 모델 다운로드는 모의 테스트로 확인했으며, macOS 실행·Chroma 호환성·검색 재현은 아직 검증하지 않았습니다.

## 기본 설치를 원천 기반 서버 구축으로 전환

- `setup_data.py`의 기본 경로는 원천 파싱→SQLite→일반/민법 인덱스 구축입니다. ZIP은 `--validation-bundle` 평가 재현 경로로 남겼습니다. Django에서는 `prepare_retrieval` 관리 명령을 사용합니다.
- Git의 승인 원천 스냅샷을 사용하므로 기존 DB·벡터·별도 ZIP 없이 새 서버에서 구축할 수 있습니다. 신규·개정 법령을 자동 채택하지 않으며, 기존 판례 시드와 안내 수집 레코드를 사용합니다.
- `9e50649`로 빈 별도 폴더에서 Django 관리 명령을 실행해 일반178·민법26·판례26·안내6청크와 기본 검색 연결을 확인했습니다. 최초 문서 임베딩은 일반/판례/안내210·민법26개입니다.
- `c19b4b1`의 명시적 재구축과 `2654264`의 출처 URL 메타데이터 갱신은 모두236개 벡터를 재사용하고 문서 임베딩0건이었습니다. 최종 일반 재실행에서는 재구축 생략·전체 적재 파일 바이트 동일·새 구축 폴더0개를 확인했습니다.
- 본문·논리 조문 범위는 기존 DB와 일치합니다. 법령 내부 청크 ID129개는 예전 순번 형식에서 현재 적재기의 조문별 형식으로 변경됐습니다. 여러 조문이 제1조 URL을 공유하던 기존 자료와 달리 재구축 출처는 조문별 판본·조·가지번호를 지정합니다.
- 전체1,168통과·3건너뜀·172subtests(`9e50649`). 이후 로그 보존·출처 URL 보완 포함 관련55통과. Django 실제 설정의 `check`도 프로세스 임시 키로 통과했습니다. 로컬 `.env`의 Django 키 미설정은 그대로이며 파일을 변경하지 않았습니다.
- [서버 구축 요약](../data/eval/patch028-rollout/server-build-summary.json)에 실행 커밋·구축/재사용 수·무변경 확인·본문 비교·한계를 기록했습니다. 기존 메인 DB는 이번 서버 구축 검증에서 교체하지 않았습니다.
- Windows의 준비된 가상환경·모델 캐시를 사용했습니다. macOS·RunPod·GPU·LLM 실제 기동과 재생성 DB의235문항 전체 순위 평가는 수행하지 않았습니다. 일반 설치의 기본 검색 확인과 고정 평가 재현을 같은 결과로 보고하지 않습니다.
