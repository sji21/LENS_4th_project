# PATCH-022: F-06 항 인용 검증

## 문제와 변경

다른 조문의 제5항이나 제3조의3의 제1항을 현재 제3조의 해당 항으로 인정하던 검증을 수정했다. 답변에서 법령명이 명시된 법령·조·가지번호를 기존 citation 모듈의 식별 규칙으로 읽고 검색 근거 citation과 정확히 연결한다. 인용 모듈 자체의 동작은 변경하지 않았다. 법령명이 빠졌거나 헤더가 citation과 충돌하면 확인 불가로 처리한다.

근거 자체의 줄 시작 원문자 항 표지를 확인한다. 첫 항부터 연속된 번호만 인정하며 인용문·코드 블록·인용 블록은 제외한다. 여러 조문 제목이 섞였거나 설명문으로 시작하는 발췌, 번호가 누락·중복된 발췌는 보수적으로 확인 불가로 처리한다. 따라서 단순 본문 속 제N항 문자열은 항 존재 증거가 아니다.

`answer.evidences` 경로를 유지해 일반 법령과 civil_laws를 모두 검사한다. 잘못된 항 인용은 기존 paragraph 이슈로 보고되어 chain과 graph의 최종 답변 보류로 이어진다.

## 범위와 한계

- 이 검증은 검색 근거에서 해당 항 번호를 확인하는 것이며 법적 주장 전체의 타당성 판단이 아니다.
- 번호 없는 조문을 제1항으로 추정하지 않는다. 평문이나 부분 발췌의 정확한 항도 확인 불가가 될 수 있다.
- 현재 Evidence에는 구조·판본 필드가 없다. DB 구조와 다른 판본을 조회하거나 위치값을 적용하지 않으므로 다른 판본 구조로 누락을 보충하지 않는다. 법령 판본의 최신성·정확성 자체를 검증한 것은 아니다.
- DB·인덱스·검색 순위·코퍼스는 변경하지 않는다. 법령 구조 파서의 DB 원문 오프셋을 검색용 본문에 재사용하지 않는다.

## 검증

합성 법령으로 정확한 항, 없는 항, 교차 인용, 다른 법령, 제3조/제3조의3/제30조, 인용문·블록·코드 속 번호, 헤더 불일치, 여러 조문 혼합, 공백 차이, 번호 없는 본문, 법령명 생략을 검사했다. 민법 별도 채널과 같은 가짜 LLM 답변에 대한 chain/graph의 answered·abstained 상태도 확인했다. 실제 법적 정답의 독립 검증은 수행하지 않았다.

전체 테스트: 799 passed, 3 skipped, 124 subtests passed. 건너뜀은 비활성 LangSmith 2건과 비공개 PDF 미지정 1건이다.


## PR #20 목록·범위 및 연혁 정정

조 번호를 반복하지 않는 항 목록(·, 쉼표, 및 등)과 범위(부터…까지, 내지, 물결표 등)의 모든 항을 검사한다. 범위의 중간 항도 확인하며 역순·현재 구조 인식 범위(1~50)를 넘는 범위는 확인 불가로 처리한다. 날짜를 포함한 이동·전문개정·본조신설·제목개정 주석만 별도 허용한다. 알 수 없는 대괄호 줄과 실제 추가 조문 헤더는 계속 거부한다.

보유 주택임대차보호법 시행령 제8조 청크의 제1·2항은 이동 연혁 주석을 유지한 상태로 직접 검증했다. 법적 내용·판본 최신성을 검증한 것은 아니다. 합성 자료에서 정상·오류 목록/범위, 중간 항 누락, 과도한 범위, 연혁 주석과 혼합 헤더 경계를 검사하고 chain·graph의 정상 answered / 없는 항 abstained를 확인했다.

통합 테스트 중 별개 기존 문제도 확인했다. `제1항부터 제2항까지에 따르면`에서 기관명 인식이 `제1항부`를 guide로 오인한다. 이번 테스트는 같은 항 목록 뒤 콜론을 사용해 항 검증 경로를 확인했으며 이 기관명 오인은 이번 범위에서 수정하지 않았다. 항 검증 단위 테스트는 해당 목록·범위 자체를 직접 검사한다.
정정 후 전체 테스트: 835 passed, 3 skipped, 124 subtests passed.


단독 `[종전 제7조는 제14조로 이동 <2013. 12. 30.>]` 형식을 추가 허용했다. 실제 보유 시행령 제7·8조에서 제1·2항을 확인했으며 다른 조문 헤더와 알 수 없는 대괄호 차단을 유지했다. 전체 테스트: 836 passed, 3 skipped, 124 subtests passed.
