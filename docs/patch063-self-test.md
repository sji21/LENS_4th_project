# PATCH-063 자체 검증 — 2026-09-22

브랜치: `fix/patch-063-ocr-improvements`. 미커밋 작업 트리를 검증했다. 사용자 대화·첨부 원본·저장 분석은 변경하지 않았다.

## 결과

| 검사 | 결과 |
| --- | --- |
| 전체 Python 회귀 검사 | 3,056 통과, 2 실패, 3 설정 오류, 12 생략, 하위 검사 689 통과 |
| JavaScript 입력창·후속 질문·요청 식별자 | 12 통과 |
| Django 시스템 검사 | 문제 없음 |
| Git diff 공백 검사 | 통과 |
| 실제 계약서 JPG 재판독 → 대화 라우터 | 입주일·임대인 성명·불확실한 임차인 성명 처리 3개 통과 |

Python 실행: `CHAT_CONVERSATION_ENABLED=false .venv/bin/python -m pytest -q` (99.15초). 기존 테스트 기본값을 유지하기 위한 테스트 프로세스 설정이며, 실행 중인 서버의 대화 기능 설정은 변경하지 않았다. 대화 기능 자체는 관련 라우터 테스트와 실제 문서 검증에서 직접 실행했다.

JavaScript 실행: `node --test tests/*.test.cjs`.

## 실패 원인

실패/오류 5개는 모두 `tests/test_patch042_gap_analysis.py`에 속하며 `Evaluator source changed`가 공통 원인이다. 저장된 PATCH-041 평가 감사 기록의 코드 해시와 현재 다음 파일의 해시가 다르다.

- `scripts/patch041_retrieval_eval.py`
- `src/retrieval/profile.py`

두 파일은 작업 트리와 HEAD의 내용이 동일하다. `scripts`, `data/eval`, 해당 테스트에는 이번 작업의 변경이 없다. 따라서 이번 OCR 변경으로 파일이 바뀐 실패가 아니라 현재 커밋의 평가 코드와 과거 평가 기록의 불일치다. 평가 기록을 임의 수정하거나 무결성 검사를 완화하지 않았다. 전체 검사 통과 상태로 보고하지 않는다.

## 실제 사진 검증

저장된 원본을 로컬 메모리에서 복호화하여 새 OCR → 문서 분석 → 세션 문서 구성 → 대화 라우터를 실행했다. 사용자 상태는 복사본을 사용했다.

- 입주일: 인도기한을 문서에서 추출하는 응답 확인.
- 임대인 성명: 사용자 제공 정답과 일치하는 이름을 문서 값으로 응답.
- 임차인 성명: 판독 불일치를 확인 요청으로 처리하며 이름을 추측하지 않음.

이 실행의 OCR 및 세 응답 처리 시간은 약 18.1초였다. 해당 세 질문에서 생성 모델/공식 검색 초기화가 호출되면 실패하도록 검사했다. 따라서 실제 생성 모델의 법률 답변 정확도를 검증한 결과는 아니다. 개인 이름·주소·금액·원본 이미지는 이 보고서와 테스트 fixture에 기록하지 않았다.

## 생략 및 한계

- LangSmith 연결 검사 2개: tracing 비활성화.
- 실제 MySQL 연결/이관 검사 9개: 별도 실행 플래그 미설정.
- 실제 등기 PDF 통합 검사 1개: `REGISTRY_SAMPLE_PDF` 미지정. 계약서 JPG 실측과 구분한다.
- 사진 전체 항목의 정답률, 모든 표 양식·필기·인장·심한 원근 변형은 검증하지 않았다.
- 브라우저를 조작하는 시각적 E2E 검사는 이번 실행에 포함하지 않았다. UI 동작은 JavaScript 자동 검사로 확인했다.
- 기존 업로드에는 새 OCR이 자동 적용되지 않으므로 재첨부가 필요하다.

커밋·푸시·병합은 수행하지 않았다.
