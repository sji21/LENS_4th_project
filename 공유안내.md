# 판례 공유 파일 — data_dev_v2

이 폴더는 기존 LENS_4th_project 저장소에 반영할 파일 묶음입니다.
독립 실행 프로젝트 전체가 아니므로 기존 저장소의 같은 상대 경로에 반영하세요.
기존 파일·설정에 다른 변경이 있으면 검토·병합하고 무조건 덮어쓰지 마세요.

## 포함 내용

1. `data/case_corpus/database/knowledge.sqlite3`: 판례 8,377건의 기존 DB.
2. `data/case_corpus/chunks/cases.jsonl`: 판례 청크 8,377개.
   법령·시행령 133개와 안내 6개 JSONL도 함께 포함됩니다.
3. `data/case_corpus/release.json`: 파일 해시, 모델 revision, 검색 정책.
4. `data/case_corpus/sample-case.json`: 실제 DB 판례·문서·청크 샘플.
5. `data/case_corpus/index/chroma_kurev1_1024/` 및 리트리버 Python 코드:
   전체 8,516개 인덱스와 기존 구조의 검색 구현.

설치·검증 코드, JSON 스키마, 관련 테스트·문서와 Git LFS 설정도 포함합니다.
현재 파일은 실제 데이터입니다. Git LFS 포인터만 복사한 것이 아닙니다.
개인 `.env`, SSH 키, 모델 가중치, 캐시, 컴퓨터별 런타임 프로필은 포함하지 않습니다.

## Git 공유 시 주의

- `.gitattributes`의 `data/case_corpus` LFS 규칙을 기존 설정과 함께 반영하세요.
- DB·JSONL·인덱스는 LFS, JSON 설정·샘플·Python 코드는 일반 Git으로 전달합니다.
- 일반 Git push와 실제 LFS 파일 업로드가 모두 성공해야 팀원이 데이터를 받습니다.
- 이 복사 작업은 커밋·푸시·원격 업로드가 아닙니다.

## 팀원 확인 순서

원격 업로드 완료 후 기존 저장소 루트에서:

```powershell
git lfs install
git pull
git lfs pull
py -3.12 setup_data.py --venv-dir C:\lens-venv-312
py -3.12 setup_data.py --venv-dir C:\lens-venv-312 --check
C:\lens-venv-312\Scripts\python.exe scripts/case_corpus_query.py --question "임대차 보증금 반환과 임대인의 상계" --k 2 --output tmp/case-query.json
```

설치기는 기존 법령·민법·안내 자료와 모듈을 준비하고 판례 배포를 검증한 뒤
로컬 경로에 맞는 `runtime-profile.json`을 생성합니다. KURE 모델은 고정 revision과
파일 해시를 사용해 별도로 준비하므로 최초 실행에는 다운로드가 필요할 수 있습니다.

DB만 확인하려면 SQLite 도구로 `knowledge.sqlite3`를 열면 됩니다.

```sql
SELECT COUNT(*) FROM cases; -- 8377
SELECT * FROM cases LIMIT 5;
```

DB 조회·판례 검색에는 Qwen이 필요 없습니다. 검색 근거로 답변을 생성할 때
Qwen이 필요합니다. 실제 Qwen 8B 응답과 전체 테스트·원격 pull 검증은 아직
완료되지 않았습니다. 상세 검사 범위·제약은 `docs/case-git-release.md`를 확인하세요.
