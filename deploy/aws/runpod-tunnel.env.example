# Install as /etc/lens/runpod-tunnel.env (root:root, 0600).
# Use the current direct SSH endpoint from the RunPod console.
# Register the EC2 tunnel public key and verify/pin the host key separately.
RUNPOD_SSH_PORT=REPLACE_WITH_DIRECT_SSH_PORT
RUNPOD_SSH_TARGET=root@REPLACE_WITH_DIRECT_SSH_HOST
