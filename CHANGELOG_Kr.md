# 변경 이력

## 2026-09-02

### 버그 수정 (Fixed)

- PATCH-046에서 처리 중 타이머와 완료 답변을 하나의 안정된 답변 슬롯에서 교체하고 완료 시점의 전체 rerun을 제거했으며, 검색 모델 준비가 끝나면 1초 주기 fragment도 중단합니다. 업로드 첨부 상태는 예약된 슬롯에서 갱신하고 입력 가능 여부는 실행 중 `submit_mode="disable"`이 제어합니다. 채팅 안쪽에 별도 스크롤을 만들지 않고 기존의 페이지 전체 대화 스크롤을 유지합니다. (commit: `c4f0767`)
- PATCH-045에서 완료된 답변과 업로드 안내를 세션 이력에 먼저 저장한 뒤 깨끗한 rerun을 수행하도록 변경했습니다. 최종 답변 메타데이터는 정식 이력 렌더러 한 곳에서만 그려 답변 완료나 이후 rerun 때 Streamlit stale 요소가 흐린 상태 배지와 응답 시간 복제본으로 남지 않게 합니다. (commit: `48a9174`)
- PATCH-044에서 Streamlit 채팅 입력창을 그리기 전에 고아·종료 업로드 작업을 정리하고, 업로드 처리의 모든 종료 경로에서 활성 작업 상태를 해제하도록 변경했습니다. 답변 또는 실패 후 입력창이 비활성화된 채 남는 문제를 방지하며 일반 답변, OCR 성공·실패, 작업 유실·종료 상태와 예상하지 못한 업로드 예외를 회귀 테스트합니다. (commit: `a7daf82`)
- PATCH-040에서 첨부 질문과 파일명을 OCR 시작 전에 세션 대화에 먼저 저장하고, 다음 Streamlit rerun에서 세션 전용 업로드 작업을 처리하도록 변경했습니다. 파일별 대기·OCR 처리 중·완료·실패 상태를 표시하고 실패 시에도 질문과 사용자용 오류를 보존합니다. 처리 중에는 채팅 입력을 비활성화하며 활성 작업 ID로 rerun 간 OCR과 사용자 메시지 중복을 방지합니다. (commit: `dcd8816`)
- PATCH-042에서 Streamlit 최초 화면의 펼쳐진 서비스 소개 카드와 채팅 입력창이 한 화면에 함께 보이도록 높이를 조정했습니다. 검색 모델 준비 상태를 카드 위로 옮기고, 접근 가능한 접기·펼치기 버튼 상태를 브라우저 세션에 유지하면서 채팅 영역 높이가 함께 바뀌게 했습니다. 소개 카드만 독립적으로 갱신하고 제목과 본문 간격을 정돈했으며, 고정 Streamlit 툴바와 좁은 화면의 Deploy 버튼이 접기·펼치기 클릭을 막지 않게 했습니다. (commit: `8c7ca3e`)

### 변경

- PATCH-041에서 파일명이나 질문 문구 대신 한 번 추출해 재사용하는 OCR 본문으로 등기사항증명서와 임대차계약서를 자동 판별하도록 변경했습니다. 모호한 문서는 계약서로 기본 처리하지 않고 확인을 요청하며, 세션 라우팅은 `등본`, `이 문서` 같은 안전한 별칭과 최근 일치 첨부를 연결하되 주민등록등본은 제외합니다. 첨부 문서 근거를 읽지 못한 경우에는 일반 공식자료 검색으로 우회하지 않고 보류합니다. (commit: `c96926e`)
- PATCH-039에서 Streamlit 채팅 이력과 입력창을 KURE-v1 초기화보다 먼저 표시하도록 변경했습니다. 캐시된 단일 백그라운드 준비 작업의 상태를 독립 갱신 영역에 표시하고, 준비 전에 들어온 질문도 같은 작업을 기다리며, 준비 실패 후에만 재시도할 수 있습니다. SentenceTransformer는 로컬 모델 캐시를 먼저 사용하고 캐시가 없을 때만 Hugging Face에 접속해 오프라인 환경의 긴 메타데이터 재시도를 피합니다. (commit: `0ca21ad`)

## 2026-09-01

### 추가

- PATCH-036에서 채팅 입력창으로 PDF·JPG·PNG 여러 개를 첨부해 세션 OCR 페이지를 검색할 수 있게 했습니다. 문서·공식 근거는 같은 LangGraph 생성·검증 정책을 통과하고 답변 출처에서는 구분하며, 문서 질의는 LangSmith 추적을 비활성화합니다. (commit: `b48637f`)

### 변경

- PATCH-035에서 설정된 RunPod Ollama를 우선 사용하고, 원격 연결 실패·모델 부재·생성 실패 시 호환되는 로컬 Ollama로 자동 전환하도록 변경했습니다. 엔드포인트 설정 방법을 문서화하고 진단 정보에 실제 사용 중인 엔드포인트도 표시합니다. (commit: `8debb71`)
- PATCH-034에서 질문을 법령·기관 안내·판례 요청 유형으로 판별하고 법령·기관 안내를 먼저 검색하도록 생성 흐름을 변경했습니다. 질문 유형에 맞는 1차 근거가 있으면 판례를 생략하고, 판례를 직접 요청하거나 1차 근거가 부족할 때만 판례를 추가해 선택된 근거만 생성·검증 단계로 전달합니다. Streamlit 시작 시 KURE-v1 검색 서비스를 한 번 사전 로딩·캐시하고, 판례·기관 안내·복수 출처·값·조건·법적 행동과 효력 질문에만 Qwen 의미 검증을 실행합니다. Dev 27문항 최종 측정은 기대 상태 88.9%, 답변 가능 문항 평균 19.3초였으며, 조건부 검증만으로 속도를 크게 줄일 수 없다는 한계와 3개 실패 원인을 문서화했습니다. (commit: `e2e84df`)

### 버그 수정 (Fixed)

- 개발자 컴퓨터에서 로컬 Ollama가 실행 중이어도 결과가 달라지지 않도록, Ollama probe 실패 테스트가 우선·fallback 후보를 모두 사용할 수 없는 상태로 명시적으로 고정하게 수정했습니다. (병합 통합)

## 2026-08-31

### 변경

- PATCH-026에서 Qwen3-8B가 사용하는 법령 상위 3건 계약에 맞춰 법령 Hybrid RRF의 `rrf_k`만 60에서 5로 조정하고, 확정일자의 효과를 묻는 질문에 법령 전용 문맥 확장을 추가했습니다. 서비스 인덱스의 법령 정답 24문항에서 Hit@1 79.2%를 유지하며 Hit@3는 91.7%에서 100%로, 법령 전용 확장 추가 후 Recall@3는 97.9%에서 100%로 개선됐습니다. 판례·안내 설정은 바꾸지 않았습니다. 문맥 규칙은 관찰한 42문항 중 `dev-001` 하나에서만 발동했으므로 일반화 근거로 주장하지 않고 효과·절차 변형 질문을 새 평가셋에 추가하는 후속 과제로 남겼습니다. (commit: pending)
- PATCH-026 리뷰에서 확인된 과도한 `"없이"` 문맥 조건을 제거했습니다. 부정 표현을 확정일자에 직접 묶고 신청·발급·서류·수수료 등의 절차 신호를 차단해, 신분증·계약서·전입신고가 없는 절차 질문에 우선변제 조문이 끼어드는 문제를 회귀 테스트로 고정했습니다. (commit: pending)

## 2026-08-30

### 추가

- 국가법령정보센터 판례 상세 응답 JSONL을 공용 SQLite·Chroma 파이프라인의 표준 `CaseRecord` JSONL로 변환하는 재현 가능한 경로를 추가했습니다. 판례 1건당 판결요지 청크 1개를 유지하고, 불완전 레코드를 제외하며, 사건번호 중복과 검토된 범위 제외를 일관되게 처리합니다. 원천 변환과 SQLite 적재는 Chroma 설치 없이도 실행할 수 있습니다. (commit: `7e178f7`)

### 수정

- PATCH-023에서 검증 원천 JSONL은 모든 API 후보 수집이 성공한 경우에만 발행하고, 실패 시 기존 파일을 보존한 채 종료 코드 1로 끝내도록 보완했습니다. 실제 달력 날짜를 검증하고, SQLite 이관 뒤 사건번호·선고일 인덱스를 모두 복구하며, 수동 검토 CSV의 승인 건만 검증 코퍼스에 추가합니다. (로컬 변경, 커밋 대기)

## 2026-08-28

### 추가

- 홀드아웃 평가셋을 봉인해 한 번 측정하고, HUG·국세청 공식 안내 원문을 수집해 적재했습니다. 안내는 법적 근거가 아니므로 법령·판례와 별도 묶음(`result.guides`)으로 반환하고 프롬프트에서도 구분합니다. 안내가 없을 때는 "전세보증금반환보증이 뭔가요?"에 엉뚱한 조문이 나가면서 `is_empty()`가 False라 보류로도 걸러지지 않았습니다. (commit: pending)

- 질문 하나에 법령 TOP5 와 판례 TOP5 를 각각 돌려주는 검색 진입점(`RetrievalService`)을 추가했습니다. 생성 쪽은 청크가 아니라 본문·출처가 붙은 `Evidence` 를 받습니다. 법령과 판례를 섞어 뽑으면 서로를 밀어내 법령 Hit@5 가 17.4%p 떨어지므로 따로 뽑습니다. (commit: pending)
- 주택 질문에서 상가건물 임대차보호법을 기본 제외하는 라우팅을 넣었습니다. 법령 133청크 중 57청크(43%)가 상가 법령이라 그대로 두면 주택 질문의 상위를 차지합니다. 평가셋 25문항 기준 Hit@1 40.0% → 76.0%. 질문에 상가 신호가 있으면 제외를 풉니다. (commit: pending)
- 검색 기본 필터에 `status=current` 를 넣어 폐지·구버전 조문이 근거로 나가지 않게 했습니다. 빈 질문은 빈 결과를 돌려줍니다 — 임베딩은 공백도 벡터로 바꿔 무관한 문서를 반환하기 때문입니다. (commit: pending)

- 제공된 주택임대차 대법원 판례 26건을 공용 SQLite 스키마에 적재하고, 법령 청크와 함께 공용 KURE Chroma 컬렉션에 넣을 수 있도록 판례 청크 병합 기능을 추가했습니다. 검증된 인용 조문 원천 데이터가 확보되기 전까지 MVP에서는 `case_law_citations`를 비워 둡니다. (commit: pending)
- BM25와 KURE-v1의 순위를 RRF로 결합한 하이브리드 검색을 추가했습니다. 두 방식이 서로 다른 문항에서 실패해, 합치자 양쪽 실패가 모두 해소되어 Hit@5 100%·MRR 0.880이 되었습니다(각각 0.847, 0.860). (commit: pending)
- Chroma 적재와 Chroma 기반 검색기를 추가하고, 같은 평가셋으로 임베딩 모델을 비교해 `nlpai-lab/KURE-v1`(1024차원)으로 확정했습니다. Hit@1 80.0%·MRR 0.860으로 `text-embedding-3-small`(52.0%·0.647)을 앞섰고, CPU에서 질의당 0.141초가 걸립니다. (commit: `39cf361`)
- 법령 원천 레코드를 관계형 DB에 적재하고 검색용 청크로 추출하는 기능을 추가했습니다. 임베딩 모델을 정하기 전에 파싱 품질을 먼저 검증할 수 있으며, 같은 레코드를 다시 적재해도 중복이 생기지 않습니다. (commit: `6261cf0`)

### 변경

- Chroma의 오래된 벡터 삭제 범위를 입력 청크의 `doc_type`으로 제한했습니다. 공용 컬렉션에서 판례만 다시 색인해도 법령이 지워지지 않으며, 유형을 넘어 삭제하려면 명시적으로 `--prune-all`을 사용합니다. (commit: pending)
- 관계형 문서와 RAG 청크의 출처 유형을 법률·시행령·시행규칙·판례·법령해석·공식 가이드로 확장하고 미지원 유형은 계속 거부하도록 변경했습니다. (commit: `8d2299e`)

## 2026-08-27

### 버그 수정 (Fixed)

- 명시적인 고대비 라이트 테마를 적용해 Streamlit의 밝은 배경에서 흰색 글자가 보이지 않던 문제를 수정했습니다. (commit: `26f8cd1`)

### 변경

- 기획 문서·참고 자료·팀 공유 PDF를 `docs/planning/`에 정리하고 운영체제 독립적인 PDF 생성기를 `scripts/`로 이동했으며 재현 가능한 경로와 의존성을 문서화했습니다. (commit: `0185314`)

### 추가

- 법령·조항·판례·공식 가이드·등기 위험 근거와 평가 정답을 연결하는 버전형 SQLite 지식 스키마, 멱등적인 Chroma 컬렉션 초기화 기능과 원문에서 Vector DB까지의 관계 설명을 추가했습니다. (commit: `0d7f02c`)
- PDF와 휴대폰 촬영 JPG·JPEG·PNG 계약서의 형식 검증·EXIF 회전 보정·로컬 OCR, 핵심 작성값·육안 확인 서명·특약 개념 점검, 등기 위험신호 연계 특약 제안, 공식 근거와 개인정보를 줄인 JSON 다운로드 기능을 추가했습니다. (commit: `3192b4b`)

## 2026-08-26

### 추가

- 등기 PDF 검증, 내장 텍스트 추출과 운영체제 독립적인 Tesseract OCR 보강 기능을 추가했습니다. (commit: `c76d2ea`)
- 개인정보가 마스킹된 근거와 소유권 제한·담보권·임차권·문서 최신성에 대한 결정론적 주의 신호 규칙을 추가했습니다. (commit: `417d86b`)
- 동의 기반 PDF 업로드, 주의 신호 근거·추가 확인사항·마스킹 미리보기와 개인정보 제외 JSON 다운로드를 제공하는 Streamlit 화면을 추가했습니다. (commit: `42c51e1`)
- OCR 분석을 LLM과 분리한 상태로 위험 신호별 RAG 검색 질의, LangGraph 전달 상태와 후속 연결 경계를 추가했습니다. (commit: `e7fbf55`)
- 등기 점검 설치·실행 문서, 선택적 실제 PDF 서비스·Streamlit 통합 테스트와 운영체제별 Tesseract 탐색 테스트를 추가했습니다. Windows 실기기 검증은 대기 중입니다. (commit: `0d93f60`)

### 수정

- PDF 페이지를 순차 렌더링한 뒤 Tesseract subprocess만 병렬 실행하도록 분리해 pypdfium2 segmentation fault를 방지했습니다. (commit: `534920d`)
